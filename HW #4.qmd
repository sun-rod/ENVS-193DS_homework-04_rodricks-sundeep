---
title: "193DS HW #4"
author: "Sundeep Rodricks"
format: docx
editor: visual
---

```{r loading-packages, eval=FALSE}
library(tidyverse)
library(here)
library(naniar)
library(skimr)
library(readr)

```

## Problem #1

1.  H0: Fish length does not predict fish weight

    H0:

    HA: Fish length does predict fish weight

    HA:

2.  

    ```{r importing-data}
    ntl6_v12 <- read_csv("ntl6_v12.csv")
    View(ntl6_v12)
    ```

    ```{r filtering-data}
    # extracting just the weight and length columns from the data frame
    fish_filtered <- ntl6_v12[, c('weight', 'length')]

    fish_filtered
    ```

    ```{r missing-data-visualization}
    gg_miss_var(fish_filtered)
    ```
